#ifndef UTILS_H
#define UTILS_H

#include <string>

/* prevent compiler warnings about unused variables */
#define UNUSED(x) ((void)(x))

#define DEBUG true
void Dprintn(const std::string & s);
void Dprintn(const int & i);

#endif
